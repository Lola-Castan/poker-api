export class Card{
    color: string
    value: string
    power: number

    constructor(color: string, value: string, power: number) { 
        this.color = color
        this.value = value
        this.power = power
    }
}